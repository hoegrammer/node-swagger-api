/**
  This is just a stubbed out version of airbnb-api-core so that I can access it relatively in this example
  app!!!  When application does not live WITHIN the airbnb-api-core repo, it should require it in package.json 
  and install it via npm install.  repo:  "airbnb-api-core":"git+ssh://git@github.com:airbnb/node-swagger-api.git"
*/
module.exports = require('../../../index');